kokoro-js 1.2.1 (Apache-2.0, https://github.com/hexgrad/kokoro), dist/kokoro.web.js, unmodified.
The game serves the voice model from public/models/ when `npm run fetch-voices` has been run
(see public/js/ui/voice.js), otherwise the browser downloads it once from Hugging Face.
